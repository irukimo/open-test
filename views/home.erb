<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0 user-scalable=0">
    <meta name="description" content="">
    <meta name="author" content="">
    <script src="http://d3js.org/d3.v3.min.js"></script>
    <script src="js/RadarChart.js"></script>
    <script src="js/WTPentagon.js"></script>
    <script src="js/basic_display.js"></script>

    <title><%=TITLE%></title>

    <!-- Bootstrap core CSS -->
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap.min.css">

    <!-- Optional theme -->
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap-theme.min.css">
    <link rel="stylesheet" type="text/css" href="css/homepage.css">
    <link href='http://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
 

    <script type="text/JavaScript">
    //<![CDATA[
    //global variables

      function guessButton(btnid, uuid, coins){
        var obj = this;
        obj.uuid = uuid;
        obj.btnelement = document.getElementById(btnid);
        obj.coins = parseInt(coins);
        obj.btnelement.onclick = function(){obj.btnClicked();}    
      }

      guessButton.prototype.btnClicked = function(){
        var obj = this;
        if(obj.coins < 50){
          alert("Not enough coins to play GUESS...");
          return;
        }
        var myform = document.forms["playform"];
        myform.uuid.value = obj.uuid;
        myform.submit();
      }

    priceOfUnlock = 5;
    function UnlockBtn(btnParentId, btnId, index, coinId, author,tester){
       this.btnParent = document.getElementById(btnParentId);
       this.btn = document.getElementById(btnId);
       this.coinLabel = document.getElementById(coinId);
       this.recordIndex = index;
       this.author = author;
       this.tester = tester;
       var obj = this;
       this.btn.onclick = function(){obj.btnClicked();}    
    }
    UnlockBtn.prototype.btnClicked = function(){
       var obj = this;
       var coinsNum = parseInt(obj.coinLabel.innerHTML);
       console.log(obj.coinLabel.innerHTML)
       console.log(coinsNum)
       if(coinsNum < priceOfUnlock){
           window.alert("sorry, not enough coins...");
           return;
       }
       obj.coinLabel.innerHTML = (coinsNum - priceOfUnlock).toString();
       obj.btnParent.removeChild(obj.btn);
       var authorTextNode = document.createTextNode(obj.author);
       obj.btnParent.appendChild(authorTextNode);
       //send request
       var myXHR = new XMLHttpRequest();
       //myXHR.open("GET", "/unlock");
       myXHR.open("POST", "/unlock" + "?tester=" + obj.tester + "&unlockID=" + obj.recordIndex);
       myXHR.onreadystatechange = function() {
           if (this.readyState != 4) {
              return;
           }
           if (this.status == 200) {
              return;
           } else {
             console.log("error occurred!");
           }
       }
       myXHR.send();
    }
    //]]>
    </script>

    <style>
        .my-first-container [class^="col-"] {
          padding-top: 10px;
          padding-bottom: 10px;
          margin-bottom: 30px;
          background-color: rgba(255,255,255,.0);
          border: 1px dashed rgba(0,0,0,.4);

          .my-inner {
            padding: 5px;
            background-color: rgba(255,255,255,.0);
            border: 1px dashed rgba(0,0,0,.4);
  }
      }
     .my-container [class^="col-"] {
          padding-top: 10px;
          padding-bottom: 10px;
          background-color: rgba(255,255,255,.0);
          border: 1px dashed rgba(0,0,0,.4);

          .my-inner {
            padding: 5px;
            background-color: rgba(255,255,255,.0);
            border: 1px dashed rgba(0,0,0,.4);
  }
      }

      #playpanel{
        background-color: #f6d0d0;
        background-image:url('/img/play.png');
        background-size: 123px 100px;
        background-repeat: no-repeat;
        background-position:center; 
        font-family: 'Open Sans', sans-serif;
      }
      #guesspanel{
        background-color: #9bf1bd;
        background-image:url('/img/guess.png');
        background-size: 123px 100px; 
        background-repeat: no-repeat;
        background-position:center;  
      }
      #viewpanel{
        background-color: #caecfa; 
        background-image:url('/img/view.png');
        background-size: 123px 100px; 
        background-repeat: no-repeat;
        background-position:center; 
      }
      #choose-section{
        padding-left: 7px;
        padding-top: 5px;
        min-width: 316px;
      }


    </style>

  </head>
<!-- NAVBAR
================================================== -->
  <body>
    

    <!-- Marketing messaging and featurettes
    ================================================== -->
    <!-- Wrap the rest of the page in another container to center all the content. -->

    <div class="container marketing">

     <script type="text/JavaScript">
    //<![CDATA[
        var medalClicked=function(){
          var myform = document.forms["rankingsform"];
          myform.submit();
        }
        var coinClicked=function(){
          alert("在玩遊戲中會得到金幣！\n可以用金幣來看朋友的性向分析哦！");
          return;
        }

        //]]>
    </script>

    <form id="nextform" role="form" action="refill" method="post">   
           <input type="hidden" name="tester" value="<%=session[:tester]%>"> 
    </form>
    <form id="rankingsform" class="form-horizontal" role="form" action="rankings" method="post">
    </form>
    <form id="playform" class="form-horizontal" role="form" action="guess" method="post">
       <input type="hidden" name="uuid" value=""> 
    </form>

  <div class="row">
    <div id="level_display" class="col-xs-5 col-sm-3" onclick="levelClicked()"><center>等級<br><font size="5pt"><%=@@level[session[:tester]]%></font> </center></div>
    <div id="medal_display" class="col-xs-6 col-sm-4" onclick="medalClicked()"><center>
        <!-- <img src="/img/trophie.png" alt="Smiley face" width="43" height="43"> -->
      </center>
    </div>
    <div id="coin_display" class="col-xs-4 col-sm-2" onclick="coinClicked()"><%= @@coins[session[:tester]] %></div>
    <!-- Optional: clear the XS cols if their content doesn't match in height -->
  </div>


  <div class="row">
    <div id="name_pentagon_background" class="col-xs-11 col-md-7" onclick="goreport()">



<div id="name_display"><%=session[:tester]%></div>
    <div id="body1">
       <div id="chart1" ></div>
    </div>
      

     <script type="text/JavaScript">
        //<![CDATA[
          new level_progress("level_display","<%=@@progress[session[:tester]]%>");

          var firstPentagon = [
              [
              {axis:"感情",value:<%=@@score_buffer[@name]["old"]["R"]%>},
              {axis:"專業",value:<%=@@score_buffer[@name]["old"]["P"]%>},
              {axis:"社交",value:<%=@@score_buffer[@name]["old"]["S"]%>}
              ],[
              {axis:"感情",value:<%=@@score_buffer[@name]["new"]["R"]%>},
              {axis:"專業",value:<%=@@score_buffer[@name]["new"]["P"]%>},
              {axis:"社交",value:<%=@@score_buffer[@name]["new"]["S"]%>}
              ]
          ];

          var oldColor1 = "#2dffc1";
          var newColor1 = "#ff8a90";
          new Pentagon(firstPentagon, '<%=@time_difference%>',oldColor1,newColor1,"body1","chart1","#body1","#chart1");

      </script>


    </div>
  </div>

  <div class="row">
    <div id="green_background" class="col-xs-11 col-md-3" onclick="gocompare()" onmouseover="" style="cursor: pointer;">
    </br>
        比一下  
    </div>
    <div id="blue_background" class="col-xs-11 col-md-3" onclick="goguess()" onmouseover="" style="cursor: pointer;">
          </br>
        猜一下
    </div>
  </div>

<%puts @parcel_array.inspect%>
<%@parcel_array.each do |parcel|%>
  <%next if @@librarian.has_played?(session[:tester], parcel[0])%>
  <%quiz = parcel[1][0]%>
  <%btnid = "button" + parcel[0]%>
  <div class="row">
    <div id="yellow_background" class="col-xs-11 col-md-7">
      <div class="col-xs-8 col-md-7">
      <b><span class="compare_author"><%=parcel[3]%></span></b>剛剛比了<b><span class="compare_option"><%=quiz["option0"]%></span></b>跟<b><span class="compare_option"><%=quiz["option1"]%></span></b>
      </div>
      <div class="col-xs-3 col-md-3">
        <font onmouseover="" style="cursor: pointer;">
          <button id="<%=btnid%>" class="orange_small_circle" name="uuid" value="<%=parcel[0]%>">去猜</button>
        </font>
      </div>
    </div>
  </div>
  <script type="text/javascript">
    //<![CDATA[
       new guessButton("<%=btnid%>","<%=parcel[0]%>","<%=@@coins[session[:tester]]%>");
    //]]>
  </script>

  <%end%>

</div>

        <form id="compareform" class="form-horizontal" role="form" action="choose_categ" method="post">
        </form>

        <form id="guessform" class="form-horizontal" role="form" action="choose_guess_categ" method="post">
        </form>

        <form id="reportform" class="form-horizontal" role="form" action="view_my_report" method="post">
        </form>


     <script type="text/JavaScript">
    //<![CDATA[
        function gocompare(){
            compareform = document.forms["compareform"];
            compareform.submit();
        }
        function goguess(){
            guessform = document.forms["guessform"];
            guessform.submit();
        }
        function goreport(){
            reportform = document.forms["reportform"];
            reportform.submit();
        }



        var isIphone;
        if( screen.width <= 640 ) {
            // coin_display.innerHTML = screen.width.toString();
            // chartelement = document.getElementById(chartID);
        }else{
            // green_background_element = document.getElementById("green_background");
            // blue_background_element = document.getElementById("green_background");
            // red_background_element = document.getElementById("red_background");
            // yellow_background_element = document.getElementById("yellow_background");
            name_pentagon_background.style.height="390px";
            name_pentagon_background.style.width="700px";
            name_display.style.width="650px";
            green_background.style.marginLeft="120px";
            green_background.style.marginRight="100px";
            // blue_background.style.width="323px";
            yellow_background.style.width="700px";
            medal_display.style.marginRight="350px";
            // name_display.style.width="380px";
            // name_display.style.marginLeft="80px";
        }


       

      function guessButton(btnid, coins){
        var obj = this;
        obj.btnelement = document.getElementById(btnid);
        obj.coins = parseInt(coins);
        obj.btnelement.onclick = function(){obj.btnClicked();}    
      }

      guessButton.prototype.btnClicked = function(){
        var obj = this;
        if(obj.coins < 50){
          alert("Not enough coins to play GUESS...");
          return;
        }
        var myform = document.forms["guessform"];
        myform.submit();
      }

        //]]>
    </script>

  <!-- <h1>Records</h1> -->

 




      <!-- FOOTER -->


    </div><!-- /.container -->


    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    
    <!-- Latest compiled and minified JavaScript -->
    <script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.3/js/bootstrap.min.js"></script>    
  </body>
</html>
